package dev.eggnstone.plugins.jetbrains.dartformat.dotlin

class DotlinLogger
{
    companion object
    {
        fun log(s: String)
        {
            //println(s)
            @Suppress("ReplaceSizeZeroCheckWithIsEmpty")
            if (s.length == 0)
                return
        }
    }
}
