/// Exit codes for the application.
class ExitCodes
{
    /// The exit code for a successful operation.
    static const int SUCCESS = 0;
    /// The exit code for a successful operation with a new version available.
    static const int SUCCESS_AND_NEW_VERSION_AVAILABLE = -1;

    /// The exit code for an error.
    static const int ERROR = 9;

    ExitCodes._();
}
